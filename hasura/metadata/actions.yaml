actions:
- name: createUser
  definition:
    kind: synchronous
    handler: '{{HASURA_ACTION_BASE_URL}}/create_user'
    forward_client_headers: true
    headers:
    - name: verification_key
      value_from_env: HASURA_EVENT_SECRET
  permissions:
  - role: user
  - role: superadmin
- name: create_circle
  definition:
    kind: synchronous
    handler: '{{HASURA_ACTION_BASE_URL}}/create_circle'
    forward_client_headers: true
    headers:
    - name: verification_key
      value_from_env: HASURA_EVENT_SECRET
  permissions:
  - role: user
  - role: superadmin
- name: upload_image
  definition:
    kind: synchronous
    handler: '{{HASURA_ACTION_BASE_URL}}/upload_image'
  permissions:
    - role: user
    - role: superadmin

custom_types:
  enums: []
  input_objects:
  - name: create_circle_input
  - name: createUserInput
  - name: upload_image_input
  objects:
  - name: upload_image_response
  - name: create_circle_response
    relationships:
    - remote_table:
        schema: public
        name: users
      name: users
      source: default
      type: array
      field_mapping:
        id: circle_id
    - remote_table:
        schema: public
        name: circles
      name: circle
      source: default
      type: object
      field_mapping:
        id: id
  - name: createUserResponse
  scalars: []
